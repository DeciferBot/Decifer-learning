"""
Decifer Learning — Content Autopilot Foundation (Sprint 2B.1)

Modules:
    coverage_scanner   — 8-state topic coverage classifier
    work_queue         — file-backed content job queue
    failure_classifier — deterministic error → action mapper
    verifier_router    — question-type → verifier-path router
    safety             — autopilot safety guards
"""
